services:
  clipper:
    build: .
    env_file:
      - path: .env
        required: false
    healthcheck:
      test: ["CMD", "obsidian-clipper", "--help"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
